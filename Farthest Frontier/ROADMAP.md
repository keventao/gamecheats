# Farthest Frontier Roadmap

## Current

- Add a current-build Mono version of FastVillagers.
- Keep the first mod small: villager movement speed and transport wagon speed.
- Record field names from `v1.1.1b (Mono)` before adding more features.

## Next

- Smoke test in-game and tune default speed values if `VillagerShoeBonusBase = 1`
  is too aggressive or too weak.
- Add a small settings UI only if MelonPreferences is not enough.
- Consider separate patches for trade wagons or storage carts after research.

## Done

- Created `Farthest Frontier/` workspace.
- Confirmed installed Mono build and MelonLoader layout.
- Confirmed current fields used by the FastVillagers-compatible patch.
